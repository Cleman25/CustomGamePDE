public class RectCollider extends Collider {
  public PShape GetBounds() {
    return null;
  }
  
  public PShape CreateShape() {
    //PShape border = new createShape(RECT, posx, posy, sizex, sizey); // ELLIPSE, RECT, ARC, TRIANGLE, SPHERE, BOX, QUAD, or LINE
    // ellipse(), rect(), arc(), triangle(), sphere(), box(), quad(), and line()
    //border.beginShape(RECT, 
    return null;
  }
}

// collision from images? draw shape based on image amd collider?
