public enum LogMode {
  DEBUG,
  PROD
}

public enum ObjectTag {
  Projectile,
  Player,
  Platform,
  Collectible
}

public enum Layer {
  Background,
  Foreground,
  Center,
  Everything,
  Nothing
}
